import "../../styles/sections/LoadingOverlay.css";

const LoadingOverlay = () => {
  return (
    <div className="loading-overlay">
      <span className="loader"></span>
    </div>
  );
};

export default LoadingOverlay;
